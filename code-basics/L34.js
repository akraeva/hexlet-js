function length(str) {
  return str.length;
}
// вместо import { length } from 'hexlet-basics/string';

const company1 = 'Apple';
const company2 = 'Samsung';

const result = length(company1) + length(company2);
console.log(result);
